# Build the Shadowline visualizer and deploy it to GitHub Pages.
#
# Repo settings -> Pages -> Build and deployment -> Source: GitHub Actions.
name: Deploy visualizer to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build visualizer
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify types
        run: npm run check

      - name: Build visualizer
        env:
          VITE_GROUND_IMAGERY_XYZ_TEMPLATE: ${{ vars.VITE_GROUND_IMAGERY_XYZ_TEMPLATE }}
          VITE_GROUND_IMAGERY_ATTRIBUTION: ${{ vars.VITE_GROUND_IMAGERY_ATTRIBUTION }}
          VITE_GROUND_IMAGERY_TILE_SIZE: ${{ vars.VITE_GROUND_IMAGERY_TILE_SIZE }}
          VITE_GROUND_IMAGERY_MAX_ZOOM: ${{ vars.VITE_GROUND_IMAGERY_MAX_ZOOM }}
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5
        with:
          path: dist/site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5
